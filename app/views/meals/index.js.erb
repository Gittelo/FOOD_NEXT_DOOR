var newCards = document.querySelectorAll('.js-card');
function refreshList(innerHTML) {
  document.querySelector('.cards-container').innerHTML = innerHTML;
}
// store meals in variable
var mealsHTML = '<%= j render @meals %>'

function refreshMap(innerHTMLMap) {
  const newMap = document.getElementById('map-container');
  newMap.innerHTML = innerHTMLMap;
}

refreshMap('<%= j render "map" %>');
document.app.initMap(document.getElementById('map'));
  if (!window.isMapOpen) {
    mealsHTML = mealsHTML.replace(/col-lg-4/g, 'col-lg-3')
  }
  console.log(mealsHTML);
  refreshList(mealsHTML);


console.log(window.isMapOpen);
